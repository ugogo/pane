import {
  colors as rawColors,
  radius as rawRadius,
  status as rawStatus,
} from './tokens.cjs';

export const colors = rawColors;
export const radius = rawRadius;
export const status = rawStatus;
