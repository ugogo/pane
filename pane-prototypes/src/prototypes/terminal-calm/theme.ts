// Single source of truth for prototype 3's accent.
// Change this one value to re-theme the whole prototype: it is injected as the
// CSS custom property `--accent` on the root (so all styles.css rules update),
// passed to the command palette / capture overlay / sparkline, and used as the
// launcher + switcher swatch via the registry.
//
// Current value is the midpoint of the Pane logo's purple→blue gradient
// (#7e14ff → #47bfff).
export const ACCENT = '#636aff';
